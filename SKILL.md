---
name: novel-creator-skill
description: 中文长篇小说全流程创作技能。支持从模糊想法到百万字路线图，低上下文开销执行章节写作，强制质量门禁（更新记忆→一致性→风格→校稿→门禁），样章风格提取与跨项目复用，剧情 RAG 检索，新手三命令快速上手（/一键开书、/继续写、/修复本章）。
---

# 小说创作技能

## 1. 核心目标

- 更少上下文开销完成长篇稳定写作（写前最多读 4 个文件）。
- 每章强制执行质量门禁，门禁未通过禁止进入下一章。
- 章节级 RAG 检索（`/剧情检索`）提升长期一致性。

## 2. 新手三命令（日常只记这三条）

| 命令 | 说明 |
|------|------|
| `/一键开书` | 输入题材与剧情种子，自动完成建模 + 建库 + 首章准备 |
| `/继续写` | 自动执行"检索→写作→门禁→索引更新"全链路 |
| `/修复本章` | 门禁失败后自动生成最短修复路径 |

新手模式：`/新手模式 开启`（默认）；高级用户：`/新手模式 关闭`

## 3. 强制章节闭环（不可跳过）

每次生成新章节后固定执行：

1. `/更新记忆`
2. `/检查一致性`
3. `/风格校准`
4. `/校稿`
5. `/门禁检查`

`/门禁检查` 未通过 → 章节为草稿，禁止进入下一章。

## 4. 低上下文策略

- 写前默认只读：`00_memory/novel_plan.md`、`00_memory/novel_state.md`。
- 新剧情优先执行 `/剧情检索`，只读取 `next_plot_context.md` 推荐的 Top 片段。
- 单章前置读取上限：**最多 4 个文件**。
- 每 10 章做一次深度压缩与深度风格校准。

## 5. 三种工作模式

1. **从模糊想法**：`/写全篇` → `/建库` → `/更新剧情索引` → `/剧情检索` → `/写作` → 章节闭环
2. **从样章仿写**：`/仿写` → `/风格提取` → `/题材选风格` → `/剧情检索` → `/写作` → 章节闭环
3. **已有项目续写**：`/续写` → `/剧情检索` → `/写作` 或 `/批量写作` → 章节闭环

详细步骤教程见 `references/tutorials.md`。

## 6. 脚本命令（核心入口）

| 脚本入口 | 用途 |
|---------|------|
| `python3 scripts/novel_flow_executor.py one-click` | `/一键开书` 执行器 |
| `python3 scripts/novel_flow_executor.py continue-write` | `/继续写` 执行器 |
| `python3 scripts/plot_rag_retriever.py build/query` | `/更新剧情索引` / `/剧情检索` |
| `python3 scripts/chapter_gate_check.py` | `/门禁检查` |
| `python3 scripts/gate_repair_plan.py` | `/修复本章` |
| `python3 scripts/benchmark_novel_flow.py` | `/评测基线` |

完整参数说明见 `references/command-playbook.md`。

## 7. 全命令表

| 命令 | 功能 | 何时使用 |
|------|------|---------|
| `/写全篇` | 模糊想法→百万字路线图 | 开新书或大纲失效重建 |
| `/一键开书` | 新手入口，自动完成开书全流程 | 第一次开项目 |
| `/继续写` | 新手日常主命令，自动串行完整章节流程 | 日常推进章节 |
| `/修复本章` | 门禁失败后自动生成修复路径 | `/门禁检查` 返回失败后 |
| `/新手模式` | 切换简化/高级交互层 | 按需切换 |
| `/拆书` | 拆解目标作品结构，提炼爽点与钩子 | 学习目标作品打法；输出含分析框架：金手指/爽点/人设/钩子四维评分 |
| `/仿写` | 提取可复用写法模板与风格特征 | 需模仿已有样章文风 |
| `/建库` | 初始化知识库与记忆系统 | 开书阶段或缺少基础文件 |
| `/写作` | 生成单章草稿并触发质量闭环 | 推进下一章正文 |
| `/续写` | 恢复会话状态并继续 | 中断后恢复项目 |
| `/批量写作` | 连续生成多章并维持一致性 | 快速推进多章 |
| `/修改章节` | 修订已写章节并级联更新记忆 | 章节返工、补设定 |
| `/更新记忆` | 同步章节变化到状态与追踪器 | 每章生成后第一步（强制） |
| `/检查一致性` | 检查剧情/设定/角色/时间线冲突 | 每章门禁第二步（强制） |
| `/风格校准` | 检测并修正文风偏移 | 每章门禁第三步（强制） |
| `/校稿` | 去AI味润色与发布前编辑 | 每章门禁第四步（强制） |
| `/门禁检查` | 脚本化校验章节是否达到发布标准 | 每章门禁最后一步（强制） |
| `/更新剧情索引` | 扫描章节建立检索索引与角色映射 | 新章通过门禁后 |
| `/剧情检索` | RAG 风格检索最相关章节与角色片段 | 每章写前；新冲突/回收伏笔时 |
| `/题材选风格` | 按题材矩阵选择基线风格 | 开书定风格或文风漂移后重定 |
| `/风格提取` | 从样章提取风格并沉淀到项目与全局库 | 用户提供样章要求"按该风格写" |
| `/风格迁移` | 将指定风格档案应用到章节草稿 | 章节风格不统一或切换目标文风 |
| `/风格库检索` | 检索可复用风格档案并排序推荐 | 新题材开书或选风格困难 |
| `/检索记忆` | 按关键词搜索记忆系统 | 定位历史伏笔、设定或角色状态 |
| `/伏笔状态` | 查看伏笔埋设/回收/超期状态 | 写作前确认伏笔债务 |
| `/角色状态` | 汇总角色当前状态与近期变化 | 群像章节前 |
| `/时间线` | 查看并校验事件时间顺序 | 跨章跳时、回忆、并行线叙事 |
| `/完整流程` | 串联拆书→仿写→建库→写作→门禁 | 一次性跑通全链路 |
| `/安装到多工具` | 安装到 Codex/Claude Code/OpenCode/Gemini CLI/Antigravity | 首次安装或升级版本 |
| `/评测基线` | 批量运行流程并输出可量化指标 | 调参后验收、版本升级对比 |
| `/联网调研` | 通用联网调研，生成搜索关键词并补充知识库 | 写前调研、知识缺口补充、手动查询特定领域资料 |
| `/一键写书` | 全自动完成整本书：调研→开书→循环写作→完成 | 用户只需提供简介和目标字数，系统全自动完成 |

## 8. 参考文档（按需读取，默认不加载）

| 文档 | 内容 |
|------|------|
| `references/command-playbook.md` | 命令完整参数与脚本用法 |
| `references/tutorials.md` | 从零开书详细教程（A/B/C 三路径） |
| `references/gate-artifacts-spec.md` | 门禁产物规范 |
| `references/million-word-roadmap.md` | 百万字路线图结构 |
| `references/genre-style-matrix.md` | 题材风格矩阵 |

## 9. 联网调研（通用能力）

`/联网调研 [主题/关键词]` —— 在任何写作场景中可用（手动写作、半自动、全自动均可调用）。

### 执行流程
1. 运行 `python3 scripts/research_agent.py plan --genre <题材> --topic "<主题>" --project-root <目录>` 获取搜索关键词和知识缺口
2. 按关键词列表逐条联网搜索（使用当前 AI 工具的搜索能力：Claude Code WebSearch、OpenCode、Codex 等）
3. 将搜索结果通过 `python3 scripts/research_agent.py store --project-root <目录> --category "<类别>" --content "<内容>"` 存入知识库
4. 可与 `/继续写` 联动：每章写前检测知识缺口并自动补充

### 调研深度
- `quick`：5条关键词，适合日常补充
- `standard`：15条关键词，适合开书前调研（默认）
- `deep`：30条关键词，适合重大设定补充

### 适配说明
- **Claude Code**：直接使用 WebSearch 工具执行搜索
- **OpenCode / Codex**：通过 AI 工具内置搜索能力执行
- **其他工具**：输出关键词列表供手动搜索

## 10. 一键写书（全自动模式）

`/一键写书 简介="..." [目标字数=200万] [调研深度=standard]`

### 执行流程
1. 解析简介，提取题材、核心冲突、主角目标
2. 运行 `/联网调研` 进行基础调研（按题材自动生成调研维度）
3. 自动执行 `/一键开书` 初始化项目（建模+建库+首章准备）
4. 循环执行（直到达到目标字数）：
   a. 分析本章知识需求，检测缺口
   b. `/联网调研` 补充缺失资料
   c. `/继续写` 完成写作+门禁
   d. 门禁失败 → 自动修复（最多3次）
   e. 每10章冲刺复盘
   f. 每卷结束输出进度报告
5. 生成完成报告

### 断点续写
中断后再次执行 `/一键写书`，系统自动从断点恢复。

### 进度查看
```
python3 scripts/auto_novel_writer.py report --project-root <目录>
```

### 支持的 LLM
- OpenAI (GPT-4 / GPT-4-Turbo)
- Anthropic (Claude 3/4)
- Kimi 2.5 (Moonshot)
- GLM-5 (智谱)
- MiniMax 2.5
- 任意 OpenAI 兼容 API

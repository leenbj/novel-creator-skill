# 📖 Novel Creator Skill v6.3（跨工具兼容版）

> 面向中文长篇小说创作的工程化技能包：低上下文写作、强制章节门禁、样章风格提取、跨项目风格复用，并支持多工具一键安装。

## 核心能力

- 模糊剧情思路 → 百万字可执行路线图
- 单章与批量写作的稳定推进
- 每章强制质量闭环，降低 AI 味与剧情漂移
- 样章风格提取与跨项目风格库复用
- 跨工具适配：Codex / Claude Code / OpenCode / Gemini CLI / Antigravity

## 强制章节闭环（发布门禁）

每次生成新章节后，必须按顺序执行：

1. `/更新记忆`
2. `/检查一致性`
3. `/风格校准`
4. `/校稿`
5. `/门禁检查`

门禁规则：`/门禁检查` 未通过，章节只能是草稿，禁止进入下一章。

## 快速开始

### 从 0 开书

1. `/写全篇`（生成路线图与核心状态）
2. `/建库`（初始化记忆与知识库）
3. `/写作`（生成章节草稿）
4. 执行章节门禁链路直到通过

### 给样章续写

1. `/仿写`
2. `/风格提取`
3. `/题材选风格`
4. `/写作` 或 `/续写`
5. 执行章节门禁链路直到通过

## 跨工具安装

统一安装脚本：`scripts/install-portable-skill.sh`

```bash
# Codex
bash scripts/install-portable-skill.sh --tool codex --force

# Claude Code
bash scripts/install-portable-skill.sh --tool claude-code --force

# OpenCode
bash scripts/install-portable-skill.sh --tool opencode --force

# Gemini CLI
bash scripts/install-portable-skill.sh --tool gemini-cli --force

# Antigravity
bash scripts/install-portable-skill.sh --tool antigravity --force
```

安装后会自动生成对应入口文件（如 `CLAUDE.md`、`GEMINI.md` 等）和 `TOOL_COMPAT.json`。

详细安装说明见：`references/multi-tool-install.md`

## 命令总览

### 创作与建模
- `/写全篇`：模糊想法开书
- `/拆书`：对标作品拆解
- `/仿写`：风格与写法学习
- `/建库`：初始化记忆与知识库
- `/写作`：单章生成
- `/续写`：恢复并继续写作
- `/批量写作`：多章连续生成
- `/修改章节`：章节修订与级联更新

### 质量门禁（每章必经）
- `/更新记忆`
- `/检查一致性`
- `/风格校准`
- `/校稿`
- `/门禁检查`

### 风格系统
- `/题材选风格`
- `/风格提取`
- `/风格迁移`
- `/风格库检索`

### 状态查询
- `/检索记忆`
- `/伏笔状态`
- `/角色状态`
- `/时间线`

兼容入口：`/完整流程`

## 目录结构（关键）

```text
novel-creator-skill/
├── SKILL.md
├── novel-creator.md
├── novel-creator.json
├── novel-analyzer.md
├── novel-analyzer.json
├── scripts/
│   ├── style_fingerprint.py
│   ├── chapter_gate_check.py
│   └── install-portable-skill.sh
├── references/
│   ├── command-playbook.md
│   ├── gate-artifacts-spec.md
│   ├── multi-tool-install.md
│   ├── million-word-roadmap.md
│   └── genre-style-matrix.md
├── templates/
└── assets/style_library/
```

## 门禁产物规范

每章在 `04_editing/gate_artifacts/<chapter_id>/` 下应产出：

- `memory_update.md`
- `consistency_report.md`
- `style_calibration.md`
- `copyedit_report.md`
- `publish_ready.md`
- `gate_result.json`

校验脚本：

```bash
python3 scripts/chapter_gate_check.py --project-root <项目目录> --chapter-file <章节文件>
```

## 低上下文策略

- 写前默认仅读取：`novel_plan.md` + `novel_state.md`
- 单章前置读取上限：4 个文件
- 扩展追踪器按需读取，不全量加载
- 每 10 章执行一次深度压缩与深度风格校准

## 版本更新

- `v6.3.0`：跨工具一键安装、入口自动适配、安装教程完善
- `v6.2.0`：章节门禁硬校验（新增 `/门禁检查`）
- `v6.1.0`：流程去重、低上下文预算策略
- `v6.0.0`：模糊想法开书、风格提取、题材选风格
- `v5.1.0`：Smart State 与长篇记忆体系升级

## License

MIT
